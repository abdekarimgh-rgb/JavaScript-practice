var prompt = require('prompt-sync')();

console.log(" programme qui demande la distance en kilomètres et la transforme en yards.");

let yards;
let km = prompt("entre la distance :");
console.log("yards = ", yards = km * 1093.61);